<%= form_for(@product_modifier) do |f| %>
  <% if @product_modifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_modifier.errors.count, "error") %> prohibited this product_modifier from being saved:</h2>

      <ul>
      <% @product_modifier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :modifier_id %><br>
    <%= f.text_field :modifier_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
